# Autonomous Paper Loop

`AutonomousPaperLoop` runs market → risk → paper execution → learning cycles.

Start: `trader cycle start --max-cycles 1`

No `WAITING_FOR_HUMAN_APPROVAL` state. Live trading cannot be enabled by the loop.
