# Learning Memory

Persistent `TradeLearningRecord` store with similarity retrieval.

- Source of truth: in-process + paper store events
- Compat export: `references/failure_log.json`
- Similar failures produce **caution**, never automatic abort
